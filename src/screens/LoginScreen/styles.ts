import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 12,
  },
  textInputContainer: {
    marginBottom: 12,
  },
  buttonContainer: {
    width: '50%',
    alignSelf: 'center',
  },
});

export default styles;
