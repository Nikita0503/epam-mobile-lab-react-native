import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  avatar: {
    width: 150,
    height: 150,
    borderRadius: 150,
    alignSelf: 'center',
  },
});

export default styles;
